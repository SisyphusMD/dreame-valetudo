<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The bundled fastboot client (PyInstaller onefile) unpacks and executes an interpreter at
       runtime, so under the hardened runtime (required for notarization) it needs these
       exceptions. disable-library-validation also lets sunxi-fel + the client load the bundled
       libusb dylib. USB access for a non-sandboxed Developer ID CLI needs no sandbox entitlement. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
